import React from 'react';

const Classes = ({pathname}) => {
    return (
        <div>
            <h2>Classes</h2>
              {pathname}
        </div>
    );
};

export default Classes;